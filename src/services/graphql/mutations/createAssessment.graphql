mutation StartAssessmentSession($input: CreateAssessmentInput!) {
    startAssessmentSession(input: $input) {
        id
        questions {
            position
            question {
                statement
            }
        }
    }
}
