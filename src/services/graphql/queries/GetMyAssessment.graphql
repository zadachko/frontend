query GetMyAssessment($assessmentId: String!) {
    getMyAssessment(assessmentId: $assessmentId) {
        title
        questions {
            position
            question {
                statement
                type
                points
                options
                correctAnswer
            }
        }
    }
}
